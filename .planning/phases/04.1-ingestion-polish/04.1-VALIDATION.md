---
phase: 4.1
slug: ingestion-polish
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-23
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run pytest tests/test_lightrag_setup.py tests/test_ingestion.py -x -q` |
| **Full suite command** | `uv run pytest -m "not integration and not gpu" -q` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_lightrag_setup.py tests/test_ingestion.py -x -q`
- **After every plan wave:** Run `uv run pytest -m "not integration and not gpu" -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 4.1-01-01 | 01 | 0 | W0 | unit | `uv run pytest tests/test_ingestion.py -x -q` | ❌ W0 | ⬜ pending |
| 4.1-01-02 | 01 | 1 | Config fix | unit | `uv run pytest tests/test_lightrag_setup.py -x -q` | ✅ | ⬜ pending |
| 4.1-02-01 | 02 | 1 | Content filter | unit | `uv run pytest tests/test_ingestion.py::test_content_type_filtering -x -q` | ❌ W0 | ⬜ pending |
| 4.1-02-02 | 02 | 1 | Enqueue/process split | unit | `uv run pytest tests/test_ingestion.py::test_enqueue_process_split -x -q` | ❌ W0 | ⬜ pending |
| 4.1-03-01 | 03 | 1 | Resume flag | unit | `uv run pytest tests/test_ingestion.py::test_resume_flag -x -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_ingestion.py` — update existing mocks from `ainsert` → `apipeline_enqueue_documents` + `apipeline_process_enqueue_documents`; add stubs for content-type filtering, enqueue/process split, and resume flag
- [ ] Verify `tests/test_lightrag_setup.py` still passes after config changes

*Wave 0 must be green before Wave 1 implementation begins.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SiliconFlow tier upgrade | L2 prerequisite | Requires human action in SiliconFlow console | Navigate to SiliconFlow billing, upgrade to L2 (¥200/month), verify TPM limit shows ~500K |
| End-to-end ingestion timing | ~15 min target | Requires real API calls and real chunk data | Run `uv run python scripts/ingest_lightrag.py data/chunks_backup.json --full`, verify completes in <20 min |
| `finalize_storages` idempotency | Resume safety | Behavior between multiple calls needs runtime observation | Run enqueue for 50 chunks, interrupt, resume — verify no duplicate entities in graph |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
