---
phase: 05.2
slug: session-and-observability-fixes
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-28
updated: 2026-03-28
---

# Phase 05.2 — Validation Strategy

> Per-phase validation contract: stable session_id for conversation memory, Langfuse v4 session attribution via metadata, history_id removal, and MemorySaver conversation history fix.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (uv run pytest) |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run pytest tests/test_session_id.py tests/test_langfuse_integration.py tests/test_conversation_memory.py -x -q` |
| **Full suite command** | `uv run pytest -m "not integration and not gpu" -q` |
| **Estimated runtime** | ~2 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick command above
- **After every plan wave:** Run full suite (excluding integration/gpu)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~2 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| UI-02/T1 | 01 | 1 | Frontend sends stable UUID via `useRef(crypto.randomUUID())` | TypeScript compile + grep | `cd frontend && npx tsc --noEmit` | ✅ | pass |
| UI-02/T1 | 01 | 1 | No `history_id` in frontend fetch body | static analysis | `grep -r "history_id" frontend/src/` (expect 0 in request path) | ✅ | pass |
| UI-02/T2 | 01 | 1 | Backend wires `session_id` → `thread_id` | unit | `uv run pytest tests/test_session_id.py::test_query_sse_uses_session_id_as_thread_id -x` | ✅ | pass |
| MON-01/T2 | 01 | 1 | `config["metadata"]["langfuse_session_id"]` present in ainvoke | unit | `uv run pytest tests/test_session_id.py::test_query_sse_passes_metadata_with_langfuse_keys -x` | ✅ | pass |
| MON-03/T2 | 01 | 1 | Same test as MON-01 (same root cause) | unit | Same command | ✅ | pass |
| Dead-field/T2 | 01 | 1 | `QueryRequest` has no `history_id` field | unit | `uv run pytest tests/test_session_id.py::test_query_request_has_no_history_id -x` | ✅ | pass |
| Langfuse-sig/T2 | 01 | 1 | `get_langfuse_handler()` has no parameters | unit | `uv run pytest tests/test_session_id.py::test_langfuse_handler_no_args_signature -x` | ✅ | pass |
| Regression/T3 | 01 | 1 | All existing Langfuse integration tests pass | regression | `uv run pytest tests/test_langfuse_integration.py -x -q` | ✅ | pass |
| MemorySaver/T1 | 02 | 1 | `conversation_history` not passed in ainvoke input | unit | `uv run pytest tests/test_conversation_memory.py -x -q` | ✅ | pass |
| MemorySaver/T2 | 02 | 1 | History accumulates across invocations via MemorySaver | unit | `uv run pytest tests/test_conversation_memory.py::test_conversation_history_accumulates_across_invocations -x` | ✅ | pass |
| MemorySaver/T3 | 02 | 1 | Different thread_ids have isolated histories | unit | `uv run pytest tests/test_conversation_memory.py::test_different_thread_ids_have_isolated_histories -x` | ✅ | pass |

---

## Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | Status |
|--------|----------|-----------|-------------------|--------|
| UI-02 | Frontend stable UUID → backend thread_id → MemorySaver continuity | unit | `uv run pytest tests/test_session_id.py tests/test_conversation_memory.py -x -q` | COVERED |
| MON-01 | Langfuse metadata keys present in ainvoke config | unit | `uv run pytest tests/test_session_id.py::test_query_sse_passes_metadata_with_langfuse_keys -x` | COVERED |
| MON-03 | Trace session grouping (same root cause as MON-01) | unit | Same command | COVERED |

---

## Manual-Only Verifications

| Behavior | How to verify | Status |
|----------|---------------|--------|
| Frontend UUID stability across turns | Start dev server, submit 2 questions in same tab, check backend logs for same `thread_id` | Verified via Playwright UAT 2026-03-28 |
| Langfuse session grouping in dashboard | Submit 2 queries, check Langfuse dashboard groups them under same session | Verified via Langfuse API GET /api/public/sessions |
| No `history_id` in request payload | Browser DevTools → Network → POST /api/query → verify body has `question` + `session_id` only | Verified via Playwright network intercept |
| Conversation memory context-aware follow-up | Ask "Apa itu BEP?" then "Berikan contoh perhitungannya" — answer must reference BEP | Verified via manual UAT 2026-03-28 |

---

## Test Run Results (Audit 2026-03-28)

```
tests/test_session_id.py          6/6 passed
tests/test_langfuse_integration.py  6/6 passed
tests/test_conversation_memory.py  6/6 passed

Total: 18 passed in 1.17s
```

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify commands
- [x] Sampling continuity: task → wave → gate
- [x] Wave 0 complete — all test files exist
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter
- [x] Plan 01 (session_id wiring + Langfuse v4): 6 session tests + 6 Langfuse tests = 12 tests
- [x] Plan 02 (MemorySaver fix): 6 conversation memory tests

---

## Validation Audit 2026-03-28

| Metric | Count |
|--------|-------|
| Gaps found | 2 |
| Resolved | 2 |
| Escalated | 0 |

**Gaps resolved:**
1. All task statuses updated from `planned` → `pass` (verification confirmed via pytest run)
2. Plan 02 (`test_conversation_memory.py`) added to verification map — was missing from original VALIDATION.md
