---
status: complete
phase: 05.2-session-and-observability-fixes
source: [05.2-01-SUMMARY.md, 05.2-02-SUMMARY.md]
started: 2026-03-28T02:30:00Z
updated: 2026-03-28T04:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Session ID Dikirim di Setiap Query
expected: |
  Buka browser, buka DevTools → Network tab. Kirim query apapun ke frontend.
  Lihat request POST /api/query. Di request body, field session_id harus berupa UUID
  (format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx), BUKAN null.
  Kirim query kedua — session_id harus SAMA dengan query pertama (stabil selama sesi browser).
result: pass
verified_by: Playwright network intercept — kedua request POST /api/query menggunakan session_id "b7acd3e7-e719-4a8b-9d2d-6fe38d9ab3d5" (UUID valid, identik, bukan null)

### 2. Conversation Memory Berfungsi (LangGraph MemorySaver)
expected: |
  Kirim query: "Apa itu break-even point?"
  Tunggu jawaban.
  Kirim follow-up: "Berikan contoh perhitungannya"
  Backend harus mengenali konteks dari pertanyaan sebelumnya (menjawab tentang BEP, bukan
  salah konteks). Ini membuktikan thread_id = session_id terhubung ke MemorySaver.
result: pass
verified_by: |
  Playwright UAT 2026-03-28 — follow-up "Berikan contoh perhitungannya" dijawab dengan
  contoh perhitungan break-even point (BEP = 1.250 unit, Rp 125.000.000), bukan depresiasi.
  Root cause: reformulate_node tidak memiliki conversation_history → fix: inject history[-10:]
  ke reformulation prompt (commit 8634648). Gap closure terkonfirmasi via manual test.

### 3. history_id Tidak Ada di Request Body
expected: |
  Di DevTools Network, buka request body POST /api/query.
  Field history_id TIDAK boleh muncul sama sekali di body.
  Hanya ada: question dan session_id.
result: pass
verified_by: Playwright network intercept — body hanya berisi {question, session_id}, tidak ada history_id

### 4. Langfuse Session Grouping
expected: |
  Langfuse dikonfigurasi (LANGFUSE_PUBLIC_KEY ada di .env).
  Setelah mengirim beberapa query, traces harus dikelompokkan under satu Session ID
  yang sama di Langfuse dashboard (bukan trace terpisah tanpa session).
result: pass
verified_by: |
  Langfuse API GET /api/public/sessions mengembalikan session "b7acd3e7-e719-4a8b-9d2d-6fe38d9ab3d5"
  yang identik dengan session_id frontend. Dua traces (BEP + follow-up) dikelompokkan
  dalam satu session dengan userId="default". Metadata langfuse_session_id terbukti bekerja.

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — semua gap tertutup]
