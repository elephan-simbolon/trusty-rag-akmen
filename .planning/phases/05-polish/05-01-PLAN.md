---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/generation/citation_builder.py
  - src/generation/generator.py
  - tests/test_generation.py
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "Citation formatted field includes author prefix when author metadata is present: 'Horngren, Cost Accounting, Chapter 5, hal. 168-170'"
    - "Citation formatted field gracefully omits author prefix when author metadata is absent: 'Cost Accounting, Chapter 5, hal. 168-170'"
    - "Generator response text does NOT contain the redundant '**Sumber Referensi:**' text block"
    - "Citation clipboard copy includes author name when available"
  artifacts:
    - path: "src/generation/citation_builder.py"
      provides: "Author-aware citation formatting"
      contains: "author"
    - path: "src/generation/generator.py"
      provides: "Clean response without duplicate citation block"
    - path: "tests/test_generation.py"
      provides: "Tests for author prefix and no citation text block"
  key_links:
    - from: "src/generation/citation_builder.py"
      to: "src/generation/generator.py"
      via: "build_citations() called at line 95"
      pattern: "build_citations"
---

<objective>
Fix backend citation formatting to include author prefix and remove the redundant plain-text citation block from generator output.

Purpose: Success criteria requires "Horngren, Cost Accounting, Chapter 5, hal. 168-172" format. Currently `build_citation()` produces "Cost Accounting, Chapter 5, hal. 168-170" (missing author). Also, `generator.py` appends a `**Sumber Referensi:**` text block that duplicates the structured CitationList UI.

Output: Updated citation_builder.py, cleaned generator.py, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-RESEARCH.md

@src/generation/citation_builder.py
@src/generation/generator.py
@tests/test_generation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add author prefix to citation_builder and pass author through build_citations</name>
  <files>src/generation/citation_builder.py, tests/test_generation.py</files>
  <behavior>
    - Test: build_citation with metadata containing author="Horngren" returns "Horngren, Cost Accounting, Chapter 5, hal. 168-170"
    - Test: build_citation with metadata missing author key returns "Cost Accounting, Chapter 5, hal. 168-170" (no leading comma)
    - Test: build_citation with metadata author="" returns "Cost Accounting, Chapter 5, hal. 168-170" (empty string treated as absent)
    - Test: build_citations returns dicts with "author" key populated from metadata
    - Test: existing test_citation_format_in_response updated to include author in mock metadata and assert author prefix in formatted output
  </behavior>
  <action>
1. Update tests/test_generation.py FIRST (RED phase):
   - In `test_citation_format_in_response`: add `"author": "Horngren"` to mock_docs metadata dict. Change assertion to: `assert formatted.startswith("Horngren, ")` and keep existing `"Cost Accounting"`, `"Chapter 5"`, `"hal. 168-170"` assertions.
   - Add new test `test_citation_format_without_author`: same as above but WITHOUT author key in metadata. Assert formatted does NOT start with comma, and still contains "Cost Accounting, Chapter 5, hal. 168-170".
   - Add new test `test_build_citations_includes_author_field`: call build_citations with author in metadata, assert result[0]["author"] == "Horngren".
   - Run tests — they MUST fail (RED).

2. Update src/generation/citation_builder.py (GREEN phase):
   - In `build_citation()`: after existing metadata.get calls (line 12-15), add: `author = metadata.get("author", "")`. Before the return on line 24, compute: `prefix = f"{author}, " if author else ""`. Change return to: `return f"{prefix}{book_title}, {chapter}, {page_ref}"`.
   - In `build_citations()`: in the citations.append dict (lines 48-55), add `"author": metadata.get("author", ""),` after the existing fields.
   - Run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd D:/trusty-rag-akmen && uv run pytest tests/test_generation.py -x -v</automated>
  </verify>
  <done>
    - build_citation with author="Horngren" returns "Horngren, Cost Accounting, Chapter 5, hal. 168-170"
    - build_citation without author returns "Cost Accounting, Chapter 5, hal. 168-170"
    - build_citations returns dicts with "author" key
    - All tests in test_generation.py pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove redundant Sumber Referensi text block from generator.py</name>
  <files>src/generation/generator.py, tests/test_generation.py</files>
  <behavior>
    - Test: generate_response output["response"] does NOT contain "**Sumber Referensi:**"
    - Test: generate_response output["response"] equals the raw LLM text (no appended citation block)
  </behavior>
  <action>
1. Update tests/test_generation.py FIRST (RED phase):
   - Add new test `test_no_citation_text_block_in_response` that mocks `src.generation.generator.generate` (the LLM call) to return a dict with `{"text": "Test response about BEP.", "usage": {"prompt_tokens": 10, "completion_tokens": 20}}`. Mock `src.generation.generator.build_citations` to return `[{"formatted": "Horngren, Cost Accounting, Chapter 5, hal. 168-170", "book_title": "Cost Accounting", "chapter": "Chapter 5", "page_start": 168, "page_end": 170, "section_path": "", "author": "Horngren"}]`. Call `generate_response(query="test", context_docs=[{"text": "doc", "metadata": {}}])`. Assert `"**Sumber Referensi:**" not in result["response"]`. Assert `result["response"] == "Test response about BEP."`.
   - Also mock `src.generation.generator.update_token_usage` as no-op to avoid Langfuse import errors.
   - Run test — it MUST fail (RED) because generator.py currently appends the citation block.

2. Update src/generation/generator.py (GREEN phase):
   - Remove lines 97-103 entirely (the `if citations:` block that builds and appends `citation_block`).
   - Change the return dict at lines 105-108 to use `response_text` directly: `return {"response": response_text, "citations": citations}`.
   - Run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd D:/trusty-rag-akmen && uv run pytest tests/test_generation.py -x -v</automated>
  </verify>
  <done>
    - generator.py no longer appends "**Sumber Referensi:**" block to response text
    - Response text is the raw LLM output without any appended citation formatting
    - All tests in test_generation.py pass including new test_no_citation_text_block_in_response
  </done>
</task>

</tasks>

<verification>
```bash
cd D:/trusty-rag-akmen && uv run pytest tests/test_generation.py -x -v
cd D:/trusty-rag-akmen && uv run pytest -m "not integration and not gpu" -x
```
</verification>

<success_criteria>
1. `build_citation({"author": "Horngren", "book_title": "Cost Accounting", "chapter": "Chapter 5", "page_start": 168, "page_end": 170})` returns "Horngren, Cost Accounting, Chapter 5, hal. 168-170"
2. `build_citation({"book_title": "Cost Accounting", "chapter": "Chapter 5", "page_start": 168, "page_end": 170})` returns "Cost Accounting, Chapter 5, hal. 168-170" (no leading comma)
3. `generate_response()` return dict `response` field contains NO `**Sumber Referensi:**` block
4. All pytest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
