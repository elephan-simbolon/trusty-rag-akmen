---
phase: 05.1-cleanup-and-hardening
plan: 02
subsystem: frontend
status: complete
completed_at: 2026-03-28
duration_minutes: 5
tags: [frontend, api, vite, typescript, flag-3]
dependency_graph:
  requires: []
  provides: [API_BASE_URL centralized constant]
  affects: [useStreamingQuery, useHistory, useHistoryActions, FeedbackButtons, HistorySidebar]
tech_stack:
  added: [frontend/src/lib/api.ts]
  patterns: [centralized API base URL with || "" fallback for production relative URLs]
key_files:
  created:
    - frontend/src/lib/api.ts
  modified:
    - frontend/src/hooks/useStreamingQuery.ts
    - frontend/src/hooks/useHistory.ts
    - frontend/src/hooks/useHistoryActions.ts
    - frontend/src/components/FeedbackButtons.tsx
    - frontend/src/components/HistorySidebar.tsx
    - frontend/src/main.tsx
decisions:
  - Used || "" (logical OR) over ?? "" (nullish coalescing) — Vite substitutes absent VITE_* vars with undefined at build time, so both work, but || "" is idiomatic for string fallbacks
  - DEV guard on main.tsx warning prevents false-positive console.error in production SPA
---

# Phase 05.1 Plan 02: Centralize VITE_API_BASE_URL (FLAG-3 fix) Summary

## One-liner

Centralized `import.meta.env.VITE_API_BASE_URL` into typed `API_BASE_URL: string` constant with `|| ""` fallback in `frontend/src/lib/api.ts`, replacing all 6 raw call sites.

## What was done

- Created `frontend/src/lib/api.ts` exporting typed `API_BASE_URL: string` constant with `|| ""` fallback
- Replaced raw `import.meta.env.VITE_API_BASE_URL` in all 5 consumer files (useStreamingQuery.ts, useHistory.ts, useHistoryActions.ts, FeedbackButtons.tsx, HistorySidebar.tsx)
- Gated `main.tsx` startup warning to `import.meta.env.DEV` — silent in production builds
- Verified SC-4 pre-satisfied: `app/` directory absent, `streamlit` not in `pyproject.toml`

## Verification results

- Zero raw `VITE_API_BASE_URL` references in hooks/ and components/ (grep clean)
- Exactly 2 remaining references: `lib/api.ts` (definition) and `main.tsx` (DEV guard)
- `npx vite build` exits 0 — "built in 5.83s", produces `frontend/dist/`
- `uv run pytest` baseline: pre-existing failures in test_qdrant_indexing.py, test_query_modes.py, test_graph_retrieve.py, test_relational_queries.py, test_synthesis_generation.py — all confirmed pre-existing, zero new regressions introduced

## FLAG-3 status

CLOSED — all call sites use centralized `API_BASE_URL` from `@/lib/api`

## Deviations from Plan

### Pre-existing issues (out of scope, logged)

**1. [Out of scope] ChatMessage.tsx TS6133 unused import**
- Found during: TypeScript compile verification
- Issue: `useCallback` declared but not used in ChatMessage.tsx
- Status: Pre-existing (confirmed by baseline stash check). Not introduced by this plan.
- Deferred to: deferred-items.md

**2. [Out of scope] Python test failures in test_qdrant_indexing.py, test_query_modes.py, etc.**
- Found during: pytest regression run
- Issue: Import errors and mock patching failures in multiple test files
- Status: Pre-existing (confirmed by baseline stash check). Not introduced by this plan.
- Deferred to: deferred-items.md

## Known Stubs

None — all API_BASE_URL call sites are wired to the centralized constant. No placeholder values remain.

## Self-Check: PASSED
