---
phase: 01-foundation
plan: "08"
type: execute
wave: 1
depends_on: ["01-07"]
files_modified:
  - src/llm/client.py
autonomous: true
gap_closure: true
requirements:
  - UI-01
must_haves:
  truths:
    - "UI menampilkan pesan error Bahasa Indonesia dalam waktu di bawah 30 detik setelah rerank() gagal — tanpa freeze berkepanjangan"
    - "rerank() menggunakan retry yang sama cepat seperti embed_query dan generate (stop=2, wait=2-10s)"
    - "Fungsi batch ingestion (embed_batch, embed_document) tetap menggunakan _RETRY_CONFIG (stop=5, wait=60-300s)"
  artifacts:
    - path: "src/llm/client.py"
      provides: "rerank() dengan dekorator @retry(**_UI_RETRY_CONFIG)"
      contains: "@retry(**_UI_RETRY_CONFIG)\ndef rerank"
  key_links:
    - from: "src/llm/client.py:rerank()"
      to: "_UI_RETRY_CONFIG"
      via: "@retry decorator di baris 139"
      pattern: "@retry\\(\\*\\*_UI_RETRY_CONFIG\\)\\ndef rerank"
---

<objective>
Tutup sisa gap UAT Test 11: ganti dekorator retry pada rerank() dari _RETRY_CONFIG (stop=5, wait=60-300s) menjadi _UI_RETRY_CONFIG (stop=2, wait=2-10s).

Purpose: Plan 07 sudah menambahkan _UI_RETRY_CONFIG dan menerapkannya pada embed_query dan generate. Namun rerank() — yang juga dipanggil dari Streamlit UI thread via rerank_node di src/agents/nodes.py — masih menggunakan _RETRY_CONFIG. Worst-case freeze dari rerank() saat ini: 5 retries x 300s = ~25 menit. Setelah fix: 2 retries x 10s = ~20 detik.

Output: src/llm/client.py baris 139 diubah dari @retry(**_RETRY_CONFIG) menjadi @retry(**_UI_RETRY_CONFIG). Satu baris perubahan.
</objective>

<execution_context>
@C:/Users/aris.simbolon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aris.simbolon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-07-SUMMARY.md

<interfaces>
<!-- State aktual src/llm/client.py setelah Plan 07 dieksekusi -->

```python
# Baris 30-50 (sudah benar setelah Plan 07):
_RETRY_CONFIG = dict(
    retry=retry_if_exception_type((Exception,)),
    stop=stop_after_attempt(5),
    wait=wait_exponential(multiplier=1, min=60, max=300),
    before_sleep=before_sleep_log(logger, logging.WARNING),
    reraise=True,
)

_UI_RETRY_CONFIG = dict(
    retry=retry_if_exception_type((Exception,)),
    stop=stop_after_attempt(2),
    wait=wait_exponential(multiplier=1, min=2, max=10),
    before_sleep=before_sleep_log(logger, logging.WARNING),
    reraise=True,
)

# Baris 139 saat ini (SALAH — yang harus diubah):
@retry(**_RETRY_CONFIG)
def rerank(query: str, documents: list[str], top_k: int = 5) -> list[dict]: ...

# Baris 139 target (BENAR):
@retry(**_UI_RETRY_CONFIG)
def rerank(query: str, documents: list[str], top_k: int = 5) -> list[dict]: ...
```

Test yang sudah ada dan harus pass setelah fix (tests/test_embedding.py, ditambahkan Plan 07):

```python
def test_embed_query_uses_ui_retry_config():
    # Memvalidasi _UI_RETRY_CONFIG: stop=2, wait min=2, max=10
    # Juga memvalidasi _RETRY_CONFIG tetap stop=5

def test_batch_functions_keep_slow_retry():
    # Memvalidasi _UI_RETRY_CONFIG != _RETRY_CONFIG (stop berbeda)
    # Jika rerank masih pakai _RETRY_CONFIG tapi _UI_RETRY_CONFIG ada,
    # test ini tetap pass — ia hanya memeriksa dua config berbeda
```

Test di tests/test_retrieval.py yang memverifikasi rerank() masih berfungsi:

```python
def test_reranker_reorders_results(mock_siliconflow):
    # Memanggil rerank_results yang memanggil llm_rerank (alias rerank dari client.py)
    # Harus tetap pass setelah perubahan dekorator
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ganti dekorator @retry(**_RETRY_CONFIG) menjadi @retry(**_UI_RETRY_CONFIG) pada rerank()</name>
  <files>src/llm/client.py</files>
  <action>
    Baca src/llm/client.py. Temukan baris 139 yang berisi:

    ```python
    @retry(**_RETRY_CONFIG)
    def rerank(query: str, documents: list[str], top_k: int = 5) -> list[dict]:
    ```

    Ganti hanya baris dekorator tersebut menjadi:

    ```python
    @retry(**_UI_RETRY_CONFIG)
    def rerank(query: str, documents: list[str], top_k: int = 5) -> list[dict]:
    ```

    Batasan ketat — JANGAN ubah apapun selain dekorator di baris 139:
    - Jangan ubah _RETRY_CONFIG (harus tetap stop=5, wait=60-300s)
    - Jangan ubah _UI_RETRY_CONFIG
    - Jangan ubah signature, docstring, atau body rerank()
    - Jangan ubah decorator pada embed_document, embed_batch (harus tetap _RETRY_CONFIG)
    - Jangan tambahkan komentar baru

    Konteks keputusan (untuk dokumentasi di commit message): rerank() dipanggil dari
    rerank_node di src/agents/nodes.py, yang dipanggil oleh LangGraph graph, yang
    dipanggil sinkron dari app/main.py via graph.invoke() di Streamlit UI thread.
    Persis seperti embed_query dan generate — ketiga fungsi ini adalah UI-facing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_embedding.py::test_embed_query_uses_ui_retry_config tests/test_embedding.py::test_batch_functions_keep_slow_retry tests/test_retrieval.py::test_reranker_reorders_results -v --tb=short</automated>
  </verify>
  <done>
    - test_embed_query_uses_ui_retry_config: PASS
    - test_batch_functions_keep_slow_retry: PASS
    - test_reranker_reorders_results: PASS
    - src/llm/client.py baris 139 berisi "@retry(**_UI_RETRY_CONFIG)" bukan "@retry(**_RETRY_CONFIG)"
    - uv run pytest -v --tb=short: semua test pass, tidak ada regresi
  </done>
</task>

</tasks>

<verification>
Setelah task selesai, jalankan seluruh test suite untuk memastikan tidak ada regresi:

```
uv run pytest -v --tb=short
```

Verifikasi struktural (satu perintah):

```
uv run python -c "
import src.llm.client as m
import inspect
src = inspect.getsource(m.rerank)
assert '_UI_RETRY_CONFIG' in src, 'FAIL: rerank masih pakai _RETRY_CONFIG'
assert m._UI_RETRY_CONFIG['stop'].max_attempt_number == 2
print('OK: rerank menggunakan _UI_RETRY_CONFIG (stop=2, max=10s)')
"
```
</verification>

<success_criteria>
Gap UAT Test 11 tertutup sepenuhnya ketika:
1. Ketiga test target PASS tanpa modifikasi test
2. Seluruh test suite PASS (tidak ada regresi dari perubahan satu baris)
3. src/llm/client.py baris 139 berisi "@retry(**_UI_RETRY_CONFIG)"
4. Worst-case UI freeze dari rerank() turun dari ~25 menit menjadi ~20 detik
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/01-foundation/01-08-SUMMARY.md` dengan:
- Gap yang ditutup: sisa UAT Test 11 — rerank() masih menggunakan _RETRY_CONFIG setelah Plan 07
- File yang dimodifikasi: src/llm/client.py (satu baris, baris 139)
- Keputusan: rerank() adalah UI-facing function karena dipanggil dari Streamlit main thread via rerank_node -> LangGraph graph -> graph.invoke()
- Test pass sebagai bukti: test_embed_query_uses_ui_retry_config, test_batch_functions_keep_slow_retry, test_reranker_reorders_results
</output>
